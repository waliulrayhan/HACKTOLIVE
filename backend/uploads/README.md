# Uploads Directory

This directory is used by Docker to store uploaded files (avatars, documents, images).

**Note:** The actual upload files are gitignored. This .gitkeep file ensures the directory structure exists in Git.
